$schema: https://json.schemastore.org/lefthook.json
pre-commit:
  commands:
    type-check:
      run: pnpm type-check
    check:
      run: pnpm check
      stage_fixed: true
    test:
      run: pnpm test
